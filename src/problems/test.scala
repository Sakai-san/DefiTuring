package problems

import scala.collection.mutable.ArrayBuffer

object test extends App{
/*
   val nbers = ArrayBuffer[Int](-7, 1, 5, 2, -4, 3, 0)
   println( nbers.slice( 0,1) ) 
   println( nbers.slice( nbers.length-1,nbers.length ) ) 
    
    def solution(A: ArrayBuffer[Int]): Int = {
        var equilibria = List[Int]()

        val size = A.length
        var currentIndex = 0
        for ( currentIndex <- (0 to size-1-1) ){
        	  println( "slice lef" + A.slice(0, currentIndex+1) )
        	  println( "slice right" + A.slice(currentIndex+1+1, size) )
        	  
          if( A.slice(0, currentIndex+1).sum == A.slice(currentIndex+1+1, size).sum  ){
        	  equilibria = equilibria ::: List (currentIndex+1)
        	  }
        }
        println( equilibria )
        if (equilibria.isEmpty) -1 else equilibria.head

  }
  
  println( solution(nbers) )
*/
  
  
  
  
}